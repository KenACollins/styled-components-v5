// ThemeProvider will pass these variables as props to every styled component when the dark theme is in effect.
// Don't confuse these properties with CSS, they are our own variable names that get passed to styled components.
const theme = {
    id: 'dark',
    primaryColor: 'black',
    secondaryColor: 'midnightblue',
    bodyBackgroundColor: 'black',
    bodyFontColor: 'white'
};

export default theme;