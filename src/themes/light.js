// ThemeProvider will pass these variables as props to every styled component when the light theme is in effect.
// Don't confuse these properties with CSS, they are our own variable names that get passed to styled components.
const theme = {
    id: 'light',
    primaryColor: '#F8049C',
    secondaryColor: '#FDD54F',
    bodyBackgroundColor: 'white',
    bodyFontColor: 'black'
};

export default theme;